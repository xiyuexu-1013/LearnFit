{
  "name": "@learnfit/core",
  "version": "1.0.0",
  "description": "Privacy-first focus estimates for verified, screen-based study tasks with a personal baseline.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "browser": "./dist/index.js",
  "sideEffects": false,
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src/core",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "npm run prepare:site && vite --open /",
    "prepare:site": "node scripts/prepare-site.mjs",
    "build:site": "npm run prepare:site && vite build",
    "build:pages": "npm run build:site && node scripts/prepare-pages-worker.mjs",
    "deploy": "npm run check && npm test && npm run build && npm run build:pages && wrangler pages deploy site-dist --project-name learnfit --branch main",
    "build:extension": "npm run prepare:site && node scripts/prepare-extension.mjs",
    "build": "esbuild src/core/index.js --bundle --format=esm --platform=browser --sourcemap --outfile=dist/index.js",
    "check": "node --check src/core/index.js && node --check src/core/tracker.js && node --check src/core/engine.js && node --check extension/chrome/task-context.js && node --check extension/chrome/quiet-console.js && node --check extension/chrome/background.js && node --check extension/chrome/popup.js && node --check extension/chrome/offscreen.js && node --check extension/chrome/sandbox.js && node --check extension/chrome/permission.js && node --check demo/app.js && node --check demo/session.js && node --check demo/workflow.js && node --check demo/research.js && node --check demo/feedback-page.js && node --check demo/admin.js && node --check pages-function/index.js && node --check pages-function/validation.js",
    "prepublishOnly": "npm run check && npm run build",
    "test": "node --test tests/*.test.js"
  },
  "keywords": [
    "eye-tracking",
    "mediapipe",
    "privacy-first",
    "productivity",
    "computer-vision",
    "browser",
    "ear",
    "blink-detection",
    "study-rhythm"
  ],
  "author": "Xiyue Xu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xiyuexu-1013/LearnFit.git"
  },
  "homepage": "https://github.com/xiyuexu-1013/LearnFit#readme",
  "bugs": {
    "url": "https://github.com/xiyuexu-1013/LearnFit/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@resvg/resvg-js": "2.6.2",
    "esbuild": "^0.25.0",
    "qrcode": "^1.5.4",
    "vite": "^7.0.0",
    "wrangler": "4.134.0"
  },
  "dependencies": {
    "@fontsource/instrument-serif": "^5.2.8",
    "@mediapipe/face_mesh": "0.4.1633559619"
  }
}
